#include <stdio.h>

int main() {
    int n, i;

    printf("Enter the number of elements in the set: ");
    scanf("%d", &n);

    int nums[n];

    printf("Enter the elements of the set:\n");
    for (i = 0; i < n; i++) {
        scanf("%d", &nums[i]);
    }

    printf("The odd/even numbers in the set are:\n");
    for (i = 0; i < n; i++) {
        if (nums[i] % 2 == 0) {
            printf("%d is even.\n", nums[i]);
        } else {
            printf("%d is odd.\n", nums[i]);
        }
    }

    return 0;
}

output:

Enter the number of elements in the set: 3 5 5 7 1
Enter the elements of the set:
The odd/even numbers in the set are:
5 is odd.
5 is odd.
7 is odd.
